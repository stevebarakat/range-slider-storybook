import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs";
import { DualRangeSlider } from "./DualRangeSlider";
import { defaultProps } from "../../shared/defaultProps";

<Meta 
  title="components/DualRangeSlider" 
  component={DualRangeSlider} 
/>

export const Template = (args) => <DualRangeSlider {...args } />


# DualRangeSlider

## A single value horizontal range slider.
A control for entering a number whose exact value is not important. Displays as
a range widget defaulting to the middle value. Used in conjunction min and max
to define the range of acceptable values.

### Basic Configuration
Default labels, not rotated, snapping, 10 step interval.

<Canvas withToolbar>
<Story 
  name="basic"
  args={{
    initialLowerValue: 20,
    initialUpperValue: 80,
    min: 0,
    max: 100,
    decimals: 0,
    step: 5,
    showTicks: true,
    snap: true,
    customLabels: [],
    showLabel: true,
    prefix: "",
    suffix: "",
    primaryColor: "hsl(196, 100%, 48%)",
    primaryColorLight: "hsl(196, 100%, 70%)",
    rotateLabel: false,
    width: 1200
  }}
>
  {Template.bind({})}
</Story>  
</Canvas>

### Configuration 2
Default labels, rotated, decimals, prefix, 100,000 step interval, smooth.

<Canvas withToolbar>
  <Story 
    name="two"
    args={{
      initialLowerValue: 357159,
      initialUpperValue: 753951,
      min: 100000,
      max: 1000000,
      decimals: 2,
      step: 100000,
      showTicks: true,
      snap: false,
      customLabels: [],
      showLabel: true,
      prefix: "$",
      suffix: "",
      primaryColor: "hsl(196, 100%, 48%)",
      primaryColorLight: "hsl(196, 100%, 70%)",
      rotateLabel: true,
      width: 1200
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Configuration 3
Custom labels, suffix, 1 step interval, smooth.

<Canvas withToolbar>
  <Story 
    name="three"
    args={{
      initialLowerValue: 284,
      initialUpperValue: 428,
      min: 200,
      max: 600,
      decimals: 0,
      step: 2,
      showTicks: true,
      snap: false,
      customLabels: [
      { 230: "Very Cool" },
      { 284: "Cool" },
      { 320: "Moderate"},
      { 374: "Moderately Hot" },
      { 428: "Hot" },
      { 464: "Very Hot"},
      { 550: "Broil"},
      ],
      showLabel: true,
      prefix: "",
      suffix: "°F",
      primaryColor: "hsl(196, 100%, 48%)",
      primaryColorLight: "hsl(196, 100%, 70%)",
      rotateLabel: false,
      width: 1200
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Configuration 4
Default labels, not rotated, snapping, 10 steps.

<Canvas withToolbar>
  <Story 
    name="four"
    args={{
      initialLowerValue: 20,
      initialUpperValue: 80,
      min: 0,
      max: 100,
      decimals: 0,
      step: 5,
      showTicks: true,
        snap: true,
      customLabels: [],
      showLabel: true,
      prefix: "",
      suffix: "",
      primaryColor: "hsl(196, 100%, 48%)",
      primaryColorLight: "hsl(196, 100%, 70%)",
      rotateLabel: false,
      width: 1200
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={DualRangeSlider} />

